# Genmix — AIGC Prompts & Tasks Manager

**Genmix** 是一款 Chrome 浏览器侧栏插件，让你在 ChatGPT、Gemini、即梦等主流 AI 平台上**统一管理提示词、批量执行任务、一键捕获结果**。

告别在多个 AI 工具间反复切换和手动复制——Genmix 帮你把 AIGC 创作流程自动化。

### ✨ 核心能力

- **📋 任务管理** — 创建、编辑、批量导入导出任务列表（支持 JSON / CSV），内置多维度过滤（状态 + 类型）、全选批量操作和拖拽排序
- **🖼️ 参考图导入** — CSV/JSON 中声明参考图文件名，导入时选择本地文件夹自动匹配，一次性为多个任务挂载参考图
- **🤖 自动执行** — 从侧栏一键向 AI 工具发送 Prompt，支持队列自动连续执行，自动重试失败任务
- **📥 结果捕获** — 扫描 AI 工具页面上的图片、视频、文本结果，批量选择后导入到对应任务中
- **🌐 多平台适配** — 支持 ChatGPT、Gemini、即梦，自动识别当前页面并切换适配器

### 🛠️ 技术栈

React 19 · TypeScript · Vite · @crxjs/vite-plugin · Tailwind CSS · Chrome Manifest V3

---

## 快速开始

> 需要 [Node.js](https://nodejs.org/) 18+

```bash
npm install
npm run dev
```

1. 打开 Chrome → `chrome://extensions` → 开启「**开发者模式**」
2. 点「**加载已解压的扩展程序**」→ 选择项目根目录
3. 代码修改后自动热更新

## 构建 & 分发

```bash
npm run build
```

将 `dist/` 文件夹发给使用者，对方在 Chrome 扩展页面「加载已解压的扩展程序」选择 `dist/` 即可。

## 自动化测试

```bash
npm test              # 运行所有测试
npm run test:headed   # 有界面模式
npm run test:debug    # 调试模式
```
